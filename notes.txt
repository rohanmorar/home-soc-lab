Things I need to learn:

- What is a SIEM?
- What does Wazuh do?
- What is Sysmon?
- What logs matter?

--------------------------------------------------

What Wazuh does:

- Collects logs from machines
- Detects suspicious activity
- Creates alerts

--------------------------------------------------

What I will do:

- Look at alerts
- Investigate what happened
- Decide if it's bad or normal
- Write down what I find

--------------------------------------------------

Windows VM:

- This is my main monitored machine
- All activity here will generate logs
- Wazuh will later monitor this system

--------------------------------------------------

Ubuntu VM (not used right now):

- Originally planned as a second monitored system
- Not required for current setup

Note:

- I am focusing on:
  - Windows VM (endpoint)
  - ubuntu-wazuh VM (SIEM)

- May add a second Linux system later as an improvement

--------------------------------------------------
NEW PROGRESS

Current Setup:

- Windows VM → endpoint (generates logs)
- ubuntu-wazuh VM → SIEM (Wazuh will be installed here)

--------------------------------------------------

Ubuntu-Wazuh VM:

- This will run Wazuh (SIEM)
- Acts as the central monitoring system
- Will receive logs from other machines
- Will generate alerts I will investigate

--------------------------------------------------

Wazuh install result:

- Installer says dashboard is available over HTTPS on port 443
- Next step is to find the IP of ubuntu-wazuh
- Then access the dashboard from browser

--------------------------------------------------

Dashboard access:

- Accessed Wazuh dashboard from host machine browser
- Used local IP (192.168.x.x)
- Connection is safe because it is on a private network

--------------------------------------------------

System roles:

Host machine:

- Used to access Wazuh dashboard
- Used to view and investigate alerts
- Acts as the SOC analyst

ubuntu-wazuh VM:

- Runs Wazuh SIEM
- Receives and analyzes logs
- Generates alerts

Windows VM:

- Endpoint being monitored
- Runs Wazuh agent
- Sends logs to SIEM

Workflow:

- Windows VM generates activity
- Agent sends logs to Wazuh
- Wazuh analyzes logs
- Alerts appear in dashboard
- I investigate from host machine

--------------------------------------------------

First log activity:

- Ran commands on Windows VM (whoami, ipconfig)
- Learned that not all commands generate logs

--------------------------------------------------

First real security event:

- Created a new user on Windows VM
- This generated security log events

--------------------------------------------------

First detected alert:

- Created a user account on Windows VM
- Wazuh generated multiple alerts:
  - User account created
  - User account changed
  - Group membership changes

Observations:

- Alerts mapped to MITRE ATT&CK techniques (T1098, T1484)
- Shows how SIEM detects potentially malicious persistence activity

Key learning:

- Real alerts come from security-relevant actions
- SIEM correlates multiple events into meaningful detections

--------------------------------------------------

Next Steps:

- Start generating and analyzing alerts
- Create custom detection rules